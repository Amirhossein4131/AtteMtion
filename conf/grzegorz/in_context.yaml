run:
  hydra:
    dir: ${oc.env:PROJECT_ROOT}/hydra/in-context/${now:%Y-%m-%d}/${now:%H-%M-%S}

trainer:
  max_epochs: 10000
  log_every_n_steps: 100
  accelerator: gpu
  devices: 1

defaults:
  - model: in_context_task
  - datamodule: in_context_datamodule

